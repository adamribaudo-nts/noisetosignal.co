<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "Noise to Signal – Bringing clarity to marketers in a noisy world" }}</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/img/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/img/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/img/favicon-16x16.png">
    <link rel="apple-touch-icon" href="/img/favicon-32x32.png">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&display=swap" rel="stylesheet">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body style="font-family: 'Lato', sans-serif;" class="bg-white text-black">


<!-- Header/Navigation -->
<div class="head">
  <div class="l">
    <div class="flex items-center space-x-3 h-full py-4 pl-6">
      <a href="/" class="flex items-center space-x-3 text-black" style="text-decoration: none;">
        <img src="/img/radar-80px-dark.png" alt="Noise to Signal" class="w-8 h-8">
        <span class="text-lg font-light">Noise to Signal</span>
      </a>
    </div>
  </div>
  <div class="r">
    <nav class="flex items-center justify-end h-full py-4 pr-6 space-x-8">
      {% if current == 'home' %}
        <span class="current-page">Home</span>
      {% else %}
        <a href="/">Home</a>
      {% endif %}
      
      {% if current == 'portfolio' %}
        <span class="current-page">Portfolio</span>
      {% else %}
        <a href="/portfolio.html">Portfolio</a>
      {% endif %}
      
      <a href="/index.html#contact" class="contact-link">Contact</a>
    </nav>
  </div>
</div>

    {{ content | safe }}


<!-- Footer -->
<footer class="py-8 px-8 text-center text-white" style="background-color: rgba(45, 45, 45, 0.8);">
  <div class="max-w-4xl mx-auto">
    <div class="mb-6">
      <img src="/img/radar-80px-light.png" alt="Noise to Signal" class="w-20 h-20 mx-auto">
    </div>
    <nav class="mb-4">
      <div class="flex flex-wrap justify-center space-x-8">
        {% if current == 'home' %}
          <span class="current-page">Home</span>
        {% else %}
          <a href="/">Home</a>
        {% endif %}
        
        {% if current == 'portfolio' %}
          <span class="current-page">Portfolio</span>
        {% else %}
          <a href="/portfolio.html">Portfolio</a>
        {% endif %}
        
        <a href="/index.html#contact">Contact</a>
      </div>
    </nav>
    <p class="text-sm text-gray-400">
      <em>Bringing clarity to marketers in a noisy world © 2025</em>
    </p>
  </div>
</footer> 

</body>
</html>
